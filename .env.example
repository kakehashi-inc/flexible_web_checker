# Django settings
DEBUG=True
SECRET_KEY=django-insecure-3=your-secret-key-here
APP_BASE_URL=http://localhost:8000

ALLOWED_HOSTS=localhost,127.0.0.1,::1

# DB_HOST省略でSQLiteを使用
DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Email Settings (for SMTP)
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=False
EMAIL_USE_SSL=False
#EMAIL_TIMEOUT=
#EMAIL_SSL_KEYFILE=
#EMAIL_SSL_CERTFILE
DEFAULT_FROM_EMAIL='"Flexible web checker" <nobody@example.local>'

# Token timeouts (seconds)
EMAIL_CONFIRMATION_TIMEOUT=3600
PASSWORD_RESET_TIMEOUT=3600

# Celery settings
CELERY_BROKER_URL=django-db

# Update check schedule (crontab format: minute hour day_of_month month day_of_week)
# Default: 0 * * * * (every hour at minute 0)
UPDATE_CHECK_SCHEDULE=0 * * * *
